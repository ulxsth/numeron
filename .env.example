# リポジトリ直下に置く（`apps/web` の Vite が envDir でここを読む）
# クライアントに載せる名前は必ず VITE_ で始める
# ローカル Supabase: `supabase start` のあと `supabase status` で URL と anon key を確認
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
